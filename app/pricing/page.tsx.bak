'use client';

import { motion } from 'framer-motion';
import Section from '@/components/shared/Section';
import GradientButton from '@/components/shared/GradientButton';
import FAQAccordion from '@/components/shared/FAQAccordion';
import { Image, Video, Sparkles } from 'lucide-react';

const features = [
  {
    icon: Image,
    title: 'Brand Visuals',
    description: 'Create stunning brand assets, logos, and marketing materials with AI-powered design'
  },
  {
    icon: Video,
    title: 'Product Creatives',
    description: 'Generate product mockups, lifestyle shots, and promotional content at scale'
  },
  {
    icon: Sparkles,
    title: 'Short-Form Content',
    description: 'Produce engaging social media content, thumbnails, and ad creatives optimized for conversion'
  }
];

const faqs = [
  {
    question: 'Do I own the rights to AI-generated content?',
    answer: 'Yes, all content we create for you is yours to use commercially. We ensure full licensing rights for your business use.'
  },
  {
    question: 'Can you match our brand style?',
    answer: 'Absolutely! We train our AI systems on your brand guidelines, colors, and style to ensure consistent, on-brand output every time.'
  },
  {
    question: 'How fast can you produce content?',
    answer: 'AI art generation is incredibly fast. We can produce dozens of variations in minutes, giving you more options to choose from and faster time-to-market.'
  }
];

export default function AIArtPage() {
  return (
    <>
      <Section className="pt-32">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5 }}
          className="max-w-4xl mx-auto text-center"
        >
          <h1 className="text-5xl md:text-6xl font-bold mb-6">
            <span className="gradient-text">AI Art & Content</span> Creation
          </h1>
          <p className="text-xl text-white/60 mb-12">
            Transform your creative vision into stunning visuals with AI-powered design and content generation.
          </p>
          <GradientButton href="/contact">
            Create Your Content
          </GradientButton>
        </motion.div>
      </Section>

      <Section>
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-4xl md:text-5xl font-bold mb-4">
            Creative <span className="gradient-text">Services</span>
          </h2>
          <p className="text-xl text-white/60 max-w-2xl mx-auto">
            Professional-grade AI content creation for modern brands
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {features.map((feature, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              className="glass-card rounded-xl p-6 hover:bg-white/10 transition-all duration-300"
            >
              <div className="mb-4 inline-flex items-center justify-center w-12 h-12 rounded-lg bg-gradient-to-br from-violet-500 to-blue-500">
                <feature.icon className="w-6 h-6 text-white" />
              </div>
              <h3 className="text-xl font-semibold mb-2">{feature.title}</h3>
              <p className="text-white/60">{feature.description}</p>
            </motion.div>
          ))}
        </div>
      </Section>

      <Section className="bg-gradient-to-b from-transparent via-violet-500/5 to-transparent">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <motion.div
            initial={{ opacity: 0, x: -20 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <h2 className="text-3xl md:text-4xl font-bold mb-6">
              Creativity at Machine Speed
            </h2>
            <p className="text-white/60 mb-4">
              Why wait weeks for a designer when you can generate hundreds of creative options in hours? Our AI art services combine cutting-edge technology with creative direction.
            </p>
            <p className="text-white/60 mb-4">
              From social media posts to product photography, email headers to ad creatives—we produce high-quality visuals that capture attention and drive engagement.
            </p>
            <p className="text-white/60">
              Perfect for startups, e-commerce brands, agencies, and anyone who needs professional content at scale.
            </p>
          </motion.div>

          <motion.div
            initial={{ opacity: 0, x: 20 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
            className="glass-card rounded-2xl p-8"
          >
            <h3 className="text-2xl font-bold mb-6">Why AI Art?</h3>
            <ul className="space-y-4">
              <li className="flex items-start gap-3">
                <div className="w-2 h-2 rounded-full bg-gradient-to-r from-violet-400 to-blue-400 mt-2" />
                <span className="text-white/80">10x faster than traditional design</span>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-2 h-2 rounded-full bg-gradient-to-r from-violet-400 to-blue-400 mt-2" />
                <span className="text-white/80">Unlimited variations and iterations</span>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-2 h-2 rounded-full bg-gradient-to-r from-violet-400 to-blue-400 mt-2" />
                <span className="text-white/80">Cost-effective scalability</span>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-2 h-2 rounded-full bg-gradient-to-r from-violet-400 to-blue-400 mt-2" />
                <span className="text-white/80">Consistent brand aesthetic</span>
              </li>
            </ul>
          </motion.div>
        </div>
      </Section>

      <Section>
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="max-w-3xl mx-auto"
        >
          <h2 className="text-4xl md:text-5xl font-bold mb-12 text-center">
            Your <span className="gradient-text">Questions</span> Answered
          </h2>
          <FAQAccordion items={faqs} />
        </motion.div>
      </Section>

      <Section>
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center max-w-3xl mx-auto glass-card rounded-2xl p-12"
        >
          <h2 className="text-3xl md:text-4xl font-bold mb-6">
            Ready to Scale Your Creative Output?
          </h2>
          <p className="text-xl text-white/60 mb-8">
            Let's bring your creative vision to life with the power of AI.
          </p>
          <GradientButton href="/contact">
            Get Started
          </GradientButton>
        </motion.div>
      </Section>
    </>
  );
}
